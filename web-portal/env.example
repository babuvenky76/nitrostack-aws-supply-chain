# Optional — variables are normally set in the monorepo `../.env` (parent `supply-chain/.env`).
# See `../.env.example` for the full list including `VITE_*` keys.

VITE_COGNITO_AUTHORITY=https://cognito-idp.us-east-2.amazonaws.com/us-east-2_XXXXXXXXX
VITE_COGNITO_WEB_CLIENT_ID=
VITE_OIDC_REDIRECT_URI=http://localhost:5174/
VITE_AWS_HTTP_API_BASE_URL=https://xxxxxxxx.execute-api.us-east-2.amazonaws.com
